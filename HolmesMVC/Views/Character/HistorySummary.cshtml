@model HolmesMVC.Models.ViewModels.CharacterHistorySummary

@if (Model.AdaptName == "Canon"){<text>Appeared @Model.Times in '@Html.ActionLink(Model.AdaptName, "Details", "Adaptation", new { urlName = Model.AdaptUrlName }, null)'.</text>}else{if (Model.ActorName == "(mentioned)"){<text>Mentioned @Model.Times on @Model.MediumName in '@Html.ActionLink(Model.AdaptName, "Details", "Adaptation", new { urlName = Model.AdaptUrlName }, null)'</text>}else {<text>Played by @Html.ActionLink(Model.ActorName, "Details", "Actor", new { urlName = Model.ActorUrlName }, null) @Model.Times on @Model.MediumName in '@Html.ActionLink(Model.AdaptName, "Details", "Adaptation", new { urlName = Model.AdaptUrlName }, null)'@if(Model.AdaptTranslation != null && Model.AdaptTranslation != string.Empty) { <span style="font-weight: normal; font-size: small;"><i>@Model.AdaptTranslation</i></span>}</text>}if (!string.IsNullOrWhiteSpace(Model.Rename)){<text>, as @Model.Rename.</text>}else{<text>.</text>}}
